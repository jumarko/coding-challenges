{:paths ["src" "resources"],
 :deps {org.clojure/clojure {:mvn/version "1.12.0-alpha12"}
        org.clojure/tools.cli {:mvn/version "1.1.230"}}

 :aliases
 {
  ;; this is a simple program - we shouldn't need a lot of memory (if it's effective)
  :dev {:jvm-opts ["-Xmx512m"]}}

;;
 }
